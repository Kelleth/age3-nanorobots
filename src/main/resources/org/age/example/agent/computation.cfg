configuration {

    computationDurationInSeconds = 10
    
    workplace {
        actions = [org.age.example.agent.ReproductionAction, org.age.example.agent.EnergyTransferAction, org.age.example.agent.DeathAction, org.age.example.agent.WorkplaceStateCheckerAction]
        
        repeat(2) { index ->
            agent {
                agentClass = org.age.example.agent.DummyAgent
            }
        }
    }
    
}
